'''
Given a decimal number as input, we need to write a program 
to convert the given decimal number into an equivalent binary number.
'''

def dec_to_bin(d):
    if d == 0:
        return 0
    else:
        return 